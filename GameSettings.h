#pragma once

namespace GameSettings {
	
	inline const char* screenName = "Game";
	inline const int screenWidth = 240;
	inline const int screenHeight = 160;

	inline const bool ENABLE_VSYNC = true;
	inline const int MAX_FPS = -1;

	inline const bool PRINT_FPS_TO_CONSOLE = false;

	inline float DELTA_TIME = 0.0f;
}
